language: ruby
rvm:
  - 2.2.2
before_install: gem install bundler -v 1.10.6
before_script:
  - psql -c 'create database amazon_test;' -U postgres
  - "bundle exec rake db:migrate RAILS_ENV=test"

script: bundle exec rspec spec -fd